#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @n: integer
 * @a integar
 *
 * Returns 0
 */

int main(void)
{
	int n;
	inta[5];
	int *p;
	

	a[2] = 1024;
	p = &n;
	*(p + 5) = 98;
	printf("a[2] = %d\n", a[2]);
	return (0);
}
