#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	scanf("%n",$n)
	if (n > 0)
		printf("%n is positive\n", n);
	else if (n < 0)
		printf("%n is negative\n", n);
	else
		printf("%n is zero\n", n);
    return (0);
}
